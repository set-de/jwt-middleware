displayName: Dynamic JWT Validation Middleware
type: middleware
import: github.com/set-de/jwt-middleware
summary: Validates JWTs for access control. Fetches keys dynamically from whitelisted issuer JWKS as needed. Supports flexible claim checks with optional wildcards. SET GmbH fork.
iconPath: jwt.png
testData:
  issuers:
    - https://auth.example.com
  secret: ThisIsAPresharedSecret
  require:
    aud: test.example.com
